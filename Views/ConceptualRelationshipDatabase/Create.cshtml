@using Sammlerplattform.Models.ConceptualRelationshipDatabase
@model ConceptualRelationshipOperationParameterModel
@using Microsoft.AspNetCore.Mvc.Localization
@inject IHtmlLocalizer<Sammlerplattform.Resources.SharedResources> Localizer

@{
    ViewData["Title"] = Localizer["ConceptRelation_Create"];
    Layout = "~/Views/Shared/_LayoutSettingsConceptualRelationship.cshtml";
}

<h3>@ViewData["Title"]</h3>
<hr />
<form asp-action="CreateSubmit" enctype="multipart/form-data">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input asp-for="Concept.CollectionAreaID" class="form-control" type="hidden" value="@Model.Concept.CollectionAreaID">
    <input asp-for="Concept.RootConceptID" class="form-control" type="hidden" value="@Model.Concept.RootConceptID">
    <div class="form-group mb-3">
        <label asp-for="Concept.Name" class="control-label"></label>
        <input asp-for="Concept.Name" class="form-control">
        <span asp-validation-for="Concept.Name" class="text-danger"></span>
    </div>
    <div class="form-group mb-3">
        <label asp-for="Concept.ConceptType" class="control-label"></label>
        <select asp-for="Concept.ConceptType"
                asp-items="Html.GetEnumSelectList<ConceptType>()"
                class="form-select">
        </select>
        <span asp-validation-for="Concept.ConceptType" class="text-danger"></span>
    </div>
    <div class="form-group mb-3">
        <label asp-for="Concept.Abbreviation" class="control-label"></label>
        <input asp-for="Concept.Abbreviation" class="form-control">
        <span asp-validation-for="Concept.Abbreviation" class="text-danger"></span>
    </div>
    <div class="form-group form-check mb-3">
        <label class="form-check-label">
            <input class="form-check-input" asp-for="Concept.IsRequired" /> @Html.DisplayNameFor(model => model.Concept.IsRequired)
        </label>
    </div>
    <div class="mb-3">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">@Localizer["Relationship"]</th>
                    <th scope="col"><th>
                </tr>
            </thead>
            <tbody id="appendConceptHere">
            </tbody>
        </table>
        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#ConceptModal">
            Beziehung hinzufügen
        </button>
    </div>
    <div class="form-group mb-3">
        <label asp-for="Concept.Description" class="control-label"></label>
        <textarea asp-for="Concept.Description" rows="3" class="form-control"></textarea>
        <span asp-validation-for="Concept.Description" class="text-danger"></span>
    </div>

    <div class="mt-4 pt-3 border-top">
        <button type="submit" class="btn btn-success btn-lg px-5">
            <i class="bi bi-check-circle"></i> @Localizer["Create"]
        </button>
        @if(Model.Concept.RootConceptID != null)
        {
            <a asp-action="IndexSpecific" asp-route-id="@Model.Concept.RootConceptID" class="btn btn-outline-secondary ms-2">
                @Localizer["Cancel"]
            </a>
        }
        else if(Model.Concept.CollectionAreaID != null)
        {
            <a asp-action="IndexSpecific" asp-route-collectionAreaID="@Model.Concept.CollectionAreaID" class="btn btn-outline-secondary ms-2">
                @Localizer["Cancel"]
            </a>
        }
        else
        {
            <a asp-action="IndexGeneral" class="btn btn-outline-secondary ms-2">
                @Localizer["Cancel"]
            </a>
        }
    </div>
</form>

<partial name="~/Views/ConceptualRelationshipDatabase/_ModalConcept.cshtml" />
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}