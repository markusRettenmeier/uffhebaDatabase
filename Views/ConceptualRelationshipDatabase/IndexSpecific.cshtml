@model IEnumerable<Sammlerplattform.Models.ConceptualRelationshipDatabase.ConceptViewModel>
@using Microsoft.AspNetCore.Mvc.Localization
@inject IHtmlLocalizer<Sammlerplattform.Resources.SharedResources> Localizer

@{
    ViewData["Title"] = @Localizer["ConceptRelation_Administration_Specific"];
    Layout = "~/Views/Shared/_LayoutSettingsConceptualRelationship.cshtml";
}

<h3>@ViewData["Title"] @Model.First(x => x.IsRootConcept).Name</h3>
<p>@Localizer["Info_Delete_NotAllowed"]</p>
<hr />
<a type="button" href="/ConceptualRelationshipDatabase/Create?ConceptViewModel.RootConceptID=@Model.First(x => x.IsRootConcept).Id&ConceptViewModel.CollectionAreaID=@Model.First(x => x.IsRootConcept).CollectionAreaID&ConceptViewModel.ConceptTypeInt=0" class="btn btn-success">@Localizer["Create"]</a>

<div class="container py-4 text-center">
    <div class="row gx-5">
        <div class="col-8">
            <div class="pe-3">
                <div id="network"></div>
                <input type="hidden" asp-for="@Model.First(x => x.IsRootConcept).Id" id="RootConceptID">
            </div>
        </div>
        <div class="col-4">
            <div class="ps-3">
                <table class="table">
                    <thead>
                        <tr>
                            <th>
                                @Localizer["ConceptName"]
                            </th>
                            <th>
                                @Localizer["Abbreviation"]
                            </th>
                            <th>
                                @Localizer["ConceptType"]
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Abbreviation)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ConceptType)
                                </td>
                                <td>
                                    <a asp-action="Edit" asp-route-id="@item.Id">@Localizer["Edit"]</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<form asp-controller="CollectionAreaDatabase" asp-action="Index">
    <partial name="~/Views/ConceptualRelationshipDatabase/_SearchByColumn.cshtml" />
</form>

<style>
    #network {
        width: 100%;
        height: 600px;
        border: 1px solid #ccc;
    }
</style>